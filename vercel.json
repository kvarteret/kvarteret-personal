{
  "buildCommand": "bun run build:assets && python scripts/prepare_vercel_static.py",
  "regions": ["arn1"],
  "functions": {
    "api/index.py": {
      "includeFiles": "app/**",
      "excludeFiles": "{data/**,tests/**,node_modules/**,.venv/**}"
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index"
    }
  ]
}
