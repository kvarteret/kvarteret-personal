# Pi Adapter

Pi should read `.agents/README.md` first and then load relevant skills from
`.agents/skills/`.

This directory is intentionally minimal. Add Pi-specific runtime wiring here only
when Pi needs configuration that cannot live in the shared `.agents` surface.
